{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug GlassOps (Runtime)",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/packages/runtime/cmd/glassops/main.go",
            "cwd": "${workspaceFolder}",
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}
